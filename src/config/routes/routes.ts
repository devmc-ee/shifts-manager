export enum ProtectedRoutesEnum {
  root = '/',
  dashboard = '/',
  team = '/team',
  teamNew = '/team/new',
  shifts = '/shifts',
  shiftNew = '/shifts/new',
  timeSheets = '/time-sheets',
  business = '/business',
  settings = '/settings',
  contacts = '/contacts',
}

export enum PublicRoutesEnum {
  login = '/login',
}
